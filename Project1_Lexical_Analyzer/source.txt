	for (int i = 0; i < substr.size(); i++) { //开始结点数目
//开始结点数目
				//开始结点数目
				if (it_input != dfa.adjList.end() && it_prestate != it_input->second.end()) {
					state = (it_input->second)[state];
					endIndex++;
					if (endIndex != substr[i].length()) {
						continue;
					}
				}